-- 메뉴 사진.
--
-- 사진이 없는 게 기본값이다. 메뉴는 사람이 손으로 넣으므로 사진까지 요구하면
-- 입력이 급격히 느려진다. 사진 없이도 메뉴명과 가격만으로 쓸모가 있어야 한다.
-- UI는 사진이 없으면 X 표시 자리를 채운다.
--
-- 주의: 남의 사진(네이버 플레이스, 블로그 등)을 가져다 쓰면 안 된다. 메뉴명과 가격은
-- 사실 정보라 저작권법 93조 4항의 보호 밖이지만, 사진은 그 자체로 저작물이다.

alter table menus add column if not exists image_url text;
